<h1> Index Boards </h1>
<p>olá games </p>

<%= link_to "new Board", new_board_path, class: "btn btn-primary" %>

<ol>
  <% @boards.each do |board| %>
    <li><%= link_to "#{board.title}", board_path(board), class: "btn btn-dark"%>
      <%= cl_image_tag board.photo.key, width: 150 %>
    </li>
  <% end %>
</ol>
